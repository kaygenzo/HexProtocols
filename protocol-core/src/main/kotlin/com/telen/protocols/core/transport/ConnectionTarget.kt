package com.telen.protocols.core.transport

/**
 * Marker for "the physical link to hold" (a BLE MAC address, a socket host, an NFC reader...),
 * owned per transport module.
 */
interface ConnectionTarget
